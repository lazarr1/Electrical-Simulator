MainWidget:

<Background>:

    canvas.before:
        Color:
            rgba: (1,1,1,1)
        Rectangle:
            pos: self.pos
            size: self.size

#Where all the components will go
<Workspace>:
    canvas.before:
        Color:
            rgba: (50, 50, 50 , 0.5)
        Rectangle:
            pos: self.pos
            size: self.size

#Draw layer
<Draw>:

    canvas:

<CircuitGraphics>:



<MainWidget>:
    Background:
        size: root.width,root.height
    
    # Draw:
    #     size: root.width,root.height

    CircuitGraphics:
        id: graphics
    #     size: root.width,root.height


    Workspace:
        id: workspace
        size: root.width/3.5,root.height

